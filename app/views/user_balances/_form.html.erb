<%= form_with(model: user_balance) do |form| %>
  <% if user_balance.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_balance.errors.count, "error") %> prohibited this user_balance from being saved:</h2>

      <ul>
        <% user_balance.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :amount, style: "display: block" %>
    <%= form.text_field :amount %>
  </div>

  <div>
    <%= form.label :currency, style: "display: block" %>
    <%= form.text_field :currency %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
